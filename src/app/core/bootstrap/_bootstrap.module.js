(function () {
    'use strict';

    // not to be confused with Twitter bootstrap, these are core config values
    // required to bootstrap this application

    angular.module('ati.core.bootstrap', []);
})();